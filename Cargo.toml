[package]
name = "iceplot"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
iced = {version = "0.8.0", default-features = false, features = ["glow", "canvas"]}
evalexpr = "9.0.0"
num_cpus = "1.0"
